@model TechInvent.DM.Models.CabinetEquipmentType

@{
    ViewData["Title"] = "Delete";
}

<h3>Уверены что хотите удалить?</h3>
<div>
    <h4>Тип оборудования</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            Название
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="IdCabinetEquipmentType" />
        <input type="submit" value="Удалить" class="btn btn-danger" />
        <a class="btn btn-primary" asp-action="Index">Назад к списку</a>
    </form>
</div>
